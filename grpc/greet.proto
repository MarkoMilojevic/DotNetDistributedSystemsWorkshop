syntax = "proto3";

service Greeter {
    rpc SayHello(HelloRequest) return (HelloReply);
}

message HelloRequest {
    string name = 1;
    string language = 2;
    string friendliness = 3;
}

message HelloReply {
    string message = 1;
}
